<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav ">
        <%category_all%>
       <% @categories.each do |category| %>
       <li><%= link_to category.name, category_path(category) %></li>
       <%end%>

        <% if admin_signed_in? %>
          <li><%= link_to 'Nova kategoria', new_category_path %></li>
          <li><%= link_to 'Novy clanok', new_post_path %></li>
          <li><%= link_to 'Ucet', edit_admin_registration_path  %></li>
          <li><%= link_to 'Odhlasit', destroy_admin_session_path, :method => :delete %></li>
          <li><%= link_to 'Kategorie', categories_path %></li>
        <% else %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
