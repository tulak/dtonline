<div class="search_form">
<%= form_tag(posts_path, method: :get) do %>
  <%= text_field_tag :search, params[:search] %>
  <%= submit_tag 'Hľadať', name: nil %>
<% end %>
</div>
<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav ">
        <%category_all%>
       <% @categories.each do |category| %>
       <li><%= link_to category.name, category_path(category) %></li>
       <%end%>

      </ul>
    </div>
    <% if admin_signed_in? %>
    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav ">



          <li><%= link_to 'Nový článok', new_post_path %></li>
          <li><%= link_to 'Kategórie', categories_path %></li>
          <li><%= link_to 'Reklamy', bigbanners_path  %></li>
          <li><%= link_to 'Účet', edit_admin_registration_path  %></li>
          <li><%= link_to 'Odhlásiť', destroy_admin_session_path, :method => :delete %></li>

      </ul>
    </div>
    <% else %>
    <% end %>
  </div>
</nav>
